#version 330
in vec4 Color;
in vec2 TexCoord0;
in float lighting;
out vec4 FragColor;
uniform sampler2D gSampler;
void main()
{
	FragColor = texture2D(gSampler, TexCoord0.xy);
  FragColor = FragColor * Color * vec4(vec3(1.0,1.0,1.0)*0.3 * lighting,1.0);
}
